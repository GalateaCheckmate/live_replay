# Live Replay

Live Replay 是基于 biliup 定制的私人自动录播与 B 站投稿工具，面向 Windows 旧电脑长期无人值守运行。

## 工作流程

1. 自动监控直播间并开始录制直播源码流。
2. 默认每 60 分钟关闭并生成一个完整视频文件。
3. 每个完整分段立即写入 SQLite 上传队列，录制线程不等待上传。
4. 第一段创建 B 站投稿；同一场直播的后续分段按顺序追加为同一稿件的后续分P。
5. 上传或提交失败时保留本地文件，并按照 1、5、15、30、60 分钟退避重试。
6. 查询远端稿件并确认对应分P已经存在、视频时长有效后，才删除本地文件。
7. 软件异常退出或电脑重启后，启动时自动恢复未完成队列。

## 默认投稿行为

- 未填写投稿类型时默认使用“转载”。
- 未填写转载来源时使用主播直播间地址。
- 未填写标题时使用：`{streamer} 直播回放 %Y-%m-%d %H-%M`。
- 直播短暂断流后，在全局 `delay` 时间内恢复时继续使用原场次和原 BV 号。

## 录像目录与磁盘保护

Windows 下检测到 D 盘时，默认录像目录为：

```text
D:\LiveReplay\Recordings
```

没有 D 盘时使用程序目录下的 `recordings`。

默认磁盘阈值：

- 剩余空间低于 100 GB：写入警告日志；
- 剩余空间低于 30 GB：不启动新的录像；
- 不会为了释放空间而删除尚未通过远端验证的文件。

可以使用环境变量覆盖：

```text
LIVE_REPLAY_OUTPUT_DIR=D:\LiveReplay\Recordings
LIVE_REPLAY_DISK_WARNING_GB=100
LIVE_REPLAY_DISK_STOP_GB=30
```

## 上传限速

上传器会根据是否正在录制动态调整：

- 正在录制：默认限制为 20 Mbps；
- 当前没有录像：默认不限速。

环境变量：

```text
LIVE_REPLAY_RECORDING_UPLOAD_LIMIT_MBPS=20
LIVE_REPLAY_UPLOAD_LIMIT_MBPS=0
```

值为 `0` 表示不限速。限速针对同一进程中的所有并发上传分片合计，而不是每个分片分别限速。

## Web 管理

侧栏中的“自动上传队列”页面提供：

- 直播场次状态；
- 分P完成进度；
- AID/BVID；
- 每个录像分段的本地路径与大小；
- 排队、上传、重试、验证和删除状态；
- 重试次数、下次重试时间和错误原因；
- 手动解除退避并立即重试。

## GitHub Actions

仓库只保留两条工作流：

- **Quick**：日常推送自动运行前端构建和 Rust 工作区编译检查；
- **Full**：手动运行完整测试、Windows Release 构建，并上传便携版 ZIP。

## 账号与版权

B 站账号继续使用上传模板中指定的 Cookie 文件。Cookie 失效时只暂停上传并持续重试，不影响直播录制。

本项目保留原 biliup 的 MIT License 和版权声明。上传他人直播内容前应取得授权，并正确选择“转载”及填写来源。
