# Live Replay

Live Replay 是基于 biliup 定制的 Windows 自动录播与 B 站投稿工具，面向旧电脑长期无人值守运行。

## 核心流程

1. 自动监控直播间并录制直播源码流。
2. 默认每 60 分钟关闭当前分段，产生一个完整录像文件。
3. 完整分段立即移动到独立安全队列目录，并同时写入文件系统 outbox 与 SQLite。
4. 上传线程与录制线程分离；网络、Cookie 或 B 站接口故障不会阻塞下一段录像。
5. 第一段创建稿件；同一直播场次的后续分段严格按照 P1、P2、P3 顺序追加。
6. 每次上传保存 B 站返回的远端 `filename`，只有远端对应分P的 `filename` 完全一致且视频时长有效，才视为验证成功。
7. 上传失败按照 1、5、15、30、60 分钟退避重试；用户点击“重试”会立即唤醒工作器。
8. 电脑断电或程序异常退出后，启动时恢复 outbox、上传、远端验证和清理状态。

## 防重复投稿

首个投稿存在一种无法完全由客户端消除的网络窗口：B站可能已经收到请求，但程序没有收到响应。

Live Replay 在这种情况下不会自动重新投稿，而是把场次标记为：

```text
submission_uncertain
```

在“自动上传队列”页面中：

- B站已经生成稿件：点击“绑定稿件”，输入 AID，可选填 BVID；
- 已经检查创作中心并确认完全没有稿件：点击“确认无稿”，再允许重新创建。

这样可以避免断电或网络超时后生成两个相同投稿。

## 投稿账号与配置快照

每个直播场次开始时会保存一份不可变投稿配置，包括：

- Cookie 文件副本；
- 投稿标题模板；
- 分区、标签、简介；
- 转载设置与来源；
- 可见范围及其他投稿字段。

快照保存在本地 `data` 目录中并被 `.gitignore` 排除。之后修改当前模板或重新登录账号，不会让旧场次错误地切换账号续传。

## 默认投稿行为

- 未填写投稿类型时默认使用“转载”；
- 未填写转载来源时使用主播直播间地址；
- 未填写标题时使用：`{streamer} 直播回放 %Y-%m-%d %H-%M`；
- 短暂断流只有在主播相同、直播标题相同且处于重连窗口内时才复用原场次；
- 默认重连窗口使用全局 `delay`，可用 `LIVE_REPLAY_RECONNECT_WINDOW_SECONDS` 覆盖。

## MP4 直封装

录像仍采用直播源码流，不转码。Windows Full 便携包会在 `live-replay.exe` 同目录自带 `ffmpeg.exe` 与 `ffprobe.exe`；运行时优先使用这两个内置程序，不要求用户安装 FFmpeg 或配置 PATH。上传前若文件不是 MP4，会调用 FFmpeg 使用：

```text
-c copy
```

进行无重编码直封装，并只映射视频和音频流。TS、M2TS、FLV 等可封装格式会产生 MP4 上传文件；直封装失败时保留源文件并重试，不会删除源录像。

## 本地文件删除策略

为了防止误删，**自动删除默认关闭**。

```text
LIVE_REPLAY_DELETE_AFTER_SUCCESS=0
```

只有显式设置为 `1`，并且同时满足下列条件，才会删除本地视频：

1. 远端稿件的对应分P存在；
2. 远端 `filename` 与本次上传返回值完全相同；
3. 远端视频时长已经有效；
4. 没有发现远端分P冲突；
5. 清理状态已经持久化到 SQLite。

弹幕 XML 默认永久保留：

```text
LIVE_REPLAY_PRESERVE_DANMAKU=1
```

需要连弹幕一起删除时才改为 `0`。因为当前功能不会把本地 XML 作为 B 站弹幕上传，不能把视频上传成功等同于弹幕已经备份。

包含自定义 `run` 或 `mv` 的上传后处理器无法证明崩溃幂等性，因此 Live Replay 会保留已验证文件，不会猜测路径后自动删除。

## 安全队列与数据库恢复

完整录像先移动到原录像磁盘中的：

```text
.live-replay-queue/<session-id>/
```

随后写入：

```text
data/replay-outbox/
data/data.sqlite3
```

SQLite 使用 WAL、外键和 30 秒锁等待。短暂写锁时会先重试；数据库暂时不可用时，文件系统 outbox 保留恢复信息。连续分段会同时检查数据库与文件系统 outbox 的最大P序号，避免重复分配同一个分P。

删除主播、投稿模板或历史记录时，数据库触发器会检查未完成场次和 outbox；仍有任务时拒绝删除，避免级联清空队列。

## 录像目录与磁盘保护

Windows 检测到 D 盘时，默认录像目录为：

```text
D:\LiveReplay\Recordings
```

没有 D 盘时使用程序目录下的 `recordings`。

```text
LIVE_REPLAY_OUTPUT_DIR=D:\LiveReplay\Recordings
LIVE_REPLAY_DISK_WARNING_GB=100
LIVE_REPLAY_DISK_STOP_GB=30
```

默认行为：

- 低于 100 GB：写入警告；
- 低于 30 GB：不启动新场次；
- 每个完整分段结束后重新检查；
- 跌破停止阈值时保住刚完成的分段，然后停止继续生成新分段；
- 永远不会通过删除未验证录像来腾空间。

## 上传限速

```text
LIVE_REPLAY_RECORDING_UPLOAD_LIMIT_MBPS=20
LIVE_REPLAY_UPLOAD_LIMIT_MBPS=0
```

- 正在录制：默认全进程上传总速率 20 Mbps；
- 没有录像：默认不限速；
- 所有文件和并发分片共享同一个限速预算，不会因三个分片变成三倍带宽；
- Live Replay 默认一次只上传一个录像文件，减少旧机械硬盘同时读写压力。

## Web 管理

“自动上传队列”页面提供：

- 场次状态与投稿状态；
- AID/BVID；
- 已验证分P、预计分P和下一个分P；
- 安全队列文件、原始路径、处理后路径和远端 `filename`；
- 排队、上传、等待重试、远端验证、冲突、保留和删除状态；
- 错误原因、重试次数和下次重试时间；
- 立即重试；
- 首稿不确定时绑定已有稿件或确认无稿。

## GitHub Actions

仓库只保留两条工作流：

- **Quick**：前端构建、Rust 格式检查、工作区编译检查；
- **Full**：完整测试、Windows Release 构建并上传便携版 ZIP。

## 版权提醒

项目保留原 biliup 的 MIT License 和版权声明。上传他人直播内容前应取得授权，并正确选择“转载”及填写来源。

## 简化版默认行为（2026-08）

- 首页直接添加主播；每个主播只有一个总开关。
- 总开关开启：持续监控、开播自动录制、每60分钟分段并自动上传。
- 总开关关闭：立即安全封段，已录内容继续上传，完成后停止关注。
- 默认标题：`{streamer} 直播回放 %Y-%m-%d %H-%M`。
- 默认投稿：三角洲行动/游戏标签、转载、来源为完整直播间链接、仅自己可见、简介为空。
- 默认不录制弹幕。
- 10分钟内恢复直播视为同一场，超过10分钟创建新稿件。
- 录制正常时上传总上限默认100 Mbps；直播下行超时会自动降至5 Mbps，恢复后自动升回。
- 磁盘低于30GB警告，低于10GB不再开始新录制。
- B站分P必须已经转码并能通过播放接口取得视频流，页面显示“已可播放”后才删除本地视频。

## 运行安全补强（2026-08-07）

- 主开关关闭产生的短尾段不再按20MB阈值删除；上传前使用 ffprobe 验证，异常文件保留并显示错误。
- 同一主播10分钟内恢复始终续接原场次，不再依赖直播标题相同。
- 每一路 HTTP-FLV/HLS 下载独立记录压力；任一路异常时上传按 5/10/25/50 Mbps 逐级恢复。
- “三角洲行动”分区必须精确匹配，找不到时禁止添加，不静默回退。
- 旧配置与主播覆盖中的弹幕选项都会强制关闭。
- 首页显示30GB空间提醒；低于10GB或无法确认空间时暂停新录制。
- B站播放接口返回后还会读取远端媒体首个数据块，成功后才删除本地录像。

